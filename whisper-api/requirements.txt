flask==3.1.3
Flask-Babel==4.0.0
gunicorn==26.0.0
requests==2.34.2
whisperx @ git+https://github.com/m-bain/whisperX.git@646f511e6bb47f8d2f0687e9976f8e7035b1e18d

# Admin UI / infrastructure
huggingface_hub>=1.14.0,<2.0.0
psutil==6.0.0

# NeMo (Parakeet ASR engine)
# nemo_toolkit[asr] pulls in torch/torchaudio (already present via whisperx),
# plus NeMo core and ASR dependencies. Adds ~2 GB to the image.
# NeMo 2.0.0 still calls the removed np.sctypes API; patched post-install
# in the Dockerfile (see sed step) because whisperx requires numpy>=2.1.
nemo_toolkit[asr]==2.7.3
